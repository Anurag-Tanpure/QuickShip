# 1. Application Name (Used by Config Server to find its specific properties file)
spring.application.name=api-gateway

# 2. Config Server Connection Details
spring.cloud.config.uri=http://localhost:8088